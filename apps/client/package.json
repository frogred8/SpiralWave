{
  "name": "@repo/client",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "start": "echo \"Client assets are served by @repo/server in release mode.\"",
    "preview": "vite preview",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@repo/shared": "*",
    "howler": "^2.2.4",
    "phaser": "^3.60.0",
    "vite": "^4.0.0"
  },
  "devDependencies": {
    "@types/howler": "^2.2.12",
    "@types/node": "^25.5.0",
    "typescript": "^5.7.3"
  }
}
