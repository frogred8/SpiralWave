{
  "name": "spiral-wave-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "packageManager": "npm@11.1.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "db": "turbo run db",
    "start": "turbo run start"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "turbo": "^2.8.20"
  }
}
